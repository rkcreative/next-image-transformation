# URL for the imgproxy server
IMGPROXY_URL=http://imgproxy:8080

# This will only allow images from example.com and coolify.io to be resized.
# Use "*" to allow all domains. Supports wildcard subdomains (e.g., "*.example.com")
ALLOWED_REMOTE_DOMAINS=example.com,coolify.io

# Cache TTL in seconds for successful image responses (default: 2592000 = 30 days)
CACHE_TTL=2592000
